# PhaStock Frontend

## Setup

1. Install dependencies:

```bash
npm install
```

2. Create env file from example and set backend API URL:

```bash
cp .env.example .env
```

`VITE_API_BASE_URL` defaults to `http://localhost:4000/api/v1`.

3. Run development server:

```bash
npm run dev
```
